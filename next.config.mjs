/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  experimental: {
    // You might need to adjust this based on specific Next.js features used
  },
};

export default nextConfig;
